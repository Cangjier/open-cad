# Manifest.json 说明
```json
{
    // webhook 插件调用信息
    "webhook":{
        // 让目标插件处理webhook信息
        "plugin":"xxx"
    },
    // 需要将消息发送到的群聊
    "wechaty":{
        "id":"xxx"
    },
    // debug 流程
    "debug":{
        // 使用目标插件调试信息
        "plugin":"xxx"
    }
}
```